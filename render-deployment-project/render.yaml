services:
  - type: web
    name: ml-deployment-demo
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
